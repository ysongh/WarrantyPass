# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Solidity build output (Foundry). Regenerated by `forge build`; the ABI the
# React app needs is synced into src/contracts/ by a script, so nothing here is
# required to build the frontend.
contracts/out
contracts/cache

# Environment
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
